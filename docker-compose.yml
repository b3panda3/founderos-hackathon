version: "3.9"

services:
  founderos:
    build: .
    container_name: founderos
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - founderos-data:/app/data
    restart: unless-stopped

volumes:
  founderos-data: